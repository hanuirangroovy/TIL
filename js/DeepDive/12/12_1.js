// 12.1 함수란?
// 12-01
// f(x, y) = x + y
function add(x, y) {
  return x + y;
}

// f(2, 5) = 7
add(2, 5); // 7

// 12-02
// 함수 정의
// function add(x, y) {
//   return x + y;
// }

// 12-03
// 함수 호출
var result = add(2, 5);

// 함수 add에 인수 2, 5를 전달하면서 호출하면 반환값 7을 반환한다.
console.log(result); // 7
