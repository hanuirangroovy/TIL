// 문 : 프로그램을 구성하는 기본 단위이자 최소 실행 단위
// 토큰 : 문법적인 의미를 가짐. 문법적으로 더 이상 나눌 수 없는 코드의 기본요소.

// 변수 선언문
var x;

// 할당문
x = 5;

// 함수 선언문
function foo() {}

// 조건문
if (x > 1) {
  console.log(x);
}

// 반복문
for (var i = 0; i < 2; i++) {
  console.log(i);
}
